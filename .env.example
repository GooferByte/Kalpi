
PORT=8080
ENV=development          # development 
LOG_LEVEL=info           # debug | info | warn | error
SESSION_TTL_HOURS=24     # how long broker sessions stay valid
MOCK_MODE=false          # true → use "mock" broker without real credentials
